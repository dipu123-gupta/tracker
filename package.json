{
  "name": "geoshare-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-server": "cd server && npm install",
    "install-client": "cd client && npm install",
    "build": "npm run install-server && npm run install-client && cd client && npm run build",
    "start": "cd server && npm start",
    "dev": "concurrently \"npm run install-server && cd server && npm run dev\" \"npm run install-client && cd client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
