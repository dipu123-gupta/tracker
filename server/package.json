{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build-all": "npm install && cd ../client && npm install && npm run build && cd ../server && rm -rf dist && mkdir -p dist && cp -r ../client/dist/* ./dist/",
    "install-all": "npm run build-all",
    "build": "npm run build-all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "axios": "^1.15.2",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^4.21.1",
    "express-rate-limit": "^8.4.0",
    "helmet": "^8.1.0",
    "mongoose": "^9.5.0",
    "socket.io": "^4.8.3",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
